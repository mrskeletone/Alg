package lab20.task4;
// По заданию должны находить мак и мин элемент ,но что тогда может быть макс элемент в String
//А если только числовые форматы то зачем обобщение? И как можно вообще реализовать сравнение для обобщеных переменных?
public class MinMax<T extends Integer> {
    private T arr[];

    public MinMax(T[] arr) {
        this.arr = arr;
    }
    public void researchMinAndMax(){
        T max,searchMax;
        for(int i=0;i<this.arr.length;i++){

        }
    }
}
